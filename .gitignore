# Internal agent/AI onboarding — not for public sharing
AGENT.md

# Local configuration — contains secrets, never commit
config.local.php

# Client photos — private data, never commit
/data/*/

# Re-include the security rule that protects the data directory
!/data/.htaccess

# Test artifacts
/test-results/
/tests/test-results/
/tests/node_modules/
/tests/fixtures/

# Audit logs — runtime output, never commit
/logs/*.log

# Local editor state
/.claude/
